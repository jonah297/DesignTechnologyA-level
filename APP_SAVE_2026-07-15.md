# D&T Hub Current App Save

Date: 2026-07-21

This file is a checkpoint handover for the current D&T Hub app state. It is intended to help future you, Codex, or another developer quickly understand what is built, where it lives, how it deploys, and what still needs attention.

## Project Locations

- Local app folder: `/Users/jonahss/Documents/DT App/DesignTechnologyA-level`
- GitHub repo: `jonah297/DesignTechnologyA-level`
- Vercel project: `design-technology-a-level`
- Firebase project: `dt-study-hub`
- Main app entry: `src/App.js`
- Firebase config: `src/firebase.js`
- Firestore security rules: `firestore.rules`
- Main styles: `src/styles.css`
- Default/fallback curriculum data: `src/data.js`

## Current Code Checkpoint

- Latest local app-code commit at this save note: `4d043f1 Improve pilot reports support automation and assignment attempts`
- This commit has passed the local app tests and production build.
- It has not been pushed to GitHub from Codex because the terminal session still needs valid GitHub authentication.
- Recent completed commits:
  - `4d043f1 Improve pilot reports support automation and assignment attempts`
  - `3b9d70a Add teacher report filters and support history`
  - `5958509 QA pilot flows and shared teacher simulation`
  - `8c7f96c Polish assignment wording`
  - `6a8be17 Improve roster and telemetry table layouts`
  - `0db289b Add teacher nudges and class renaming`
  - `02f50c7 Add prep strips previews ranks and curriculum import`
  - `1d10547 Improve teacher prep simulation controls`
  - `59ea168 Keep simulation controls active across views`
  - `2380568 Enhance admin simulation lab`
  - `bfe79a6 Add admin simulation lab`

## Verification State

- `npm test -- --watchAll=false` passed on 2026-07-21.
- `npm run build` compiled successfully on 2026-07-21.
- `git diff --check` passed before the latest local commit.
- Firestore rules are present in `firestore.rules`. If rules are edited again, redeploy them after pushing the app code.

## Security Notes

- Do not commit real secrets.
- The super-admin gate uses `REACT_APP_SUPER_ADMIN_KEY`.
- The actual admin key is intentionally not written in this save note, not committed, and should stay in local `.env.local` and Vercel Environment Variables only.
- The root admin user document is expected to exist in Firestore `users/{email}` with role `admin`.
- Root-admin simulation and preview modes are designed not to write mock performance metrics to production learner progress.

## Features Currently Built

### Student Experience

- Main menu with Learn, Quiz, Refresh, Match, Insights, Blitz, and Leaderboard.
- Flashcard quizzes and written-answer flow.
- Active Assignments widgets on dashboard, quiz, written quiz, and blitz views.
- Assignment loading routes students directly into the required cards or long-answer question.
- Assignment completion requires reaching the teacher target mastery percentage.
- XP uses active engagements rather than raw stopwatch time.
- Student rank display and top-three leaderboard badges.
- Student Teacher Messages panel for automated reminders/rewards, with new/read message separation.
- Flag Error flow writes reports to `flagged_content`.

### Teacher Experience

- Educator Command Center with classes, student counts, subject access, and active assignment completion.
- Multi-class teacher schema.
- Class display-name editing.
- License and seat management UI for licensed teachers/admins.
- Assignment Engine supports:
  - whole chapter assignments,
  - subsection assignments,
  - long-answer assignments,
  - target mastery percentage,
  - deadline editing,
  - canceling active assignments.
- Student Progress Overview table is collapsible and horizontally scrollable.
- Teachers can view/select students and inspect mastery breakdowns.
- Teacher reports can be filtered by subject, assignment, due-date window, assignment status, mastery track, and last activity.
- Teachers can copy the filtered class report as a readable summary or CSV without exporting student emails from the table.
- Student detail views show the student's email only after selection, plus a parents' evening snapshot and support status.
- Account Managers can configure automated nudge/reward rules, message templates, and future timing limits such as quiet hours and weekdays only.
- Assignment attempt summaries are now tracked separately from general study progress.

### Super Admin Experience

- Hidden admin control panel behind `REACT_APP_SUPER_ADMIN_KEY`.
- Admin can preview clean student and teacher layouts without a simulation.
- Admin Simulation Lab can generate random schools with teachers, classes, and students.
- Simulation supports:
  - multiple classes and teachers,
  - teacher with multiple classes,
  - up to 365 simulated days,
  - real-time-style speed multiplier,
  - persistent play/pause/speed dock across simulation views,
  - live student telemetry,
  - collapsible telemetry table,
  - automatic idle nudges, decay warnings, assignment reminders, and streak rewards,
  - CSV-style data table export for analysis.

### Curriculum / White-Label Foundation

- Firestore-backed `curriculums/{subjectId}` collection.
- Legacy `data.js` remains as fallback and seed data.
- Admin Curriculum Editor can edit question text, answers, mark schemes, marks, and image URLs.
- Immutable question IDs are preserved so historical mastery keys remain stable.
- Bulk curriculum import path exists through the admin curriculum area.
- Firestore-backed `licenses/{licenseId}` collection supports schools, unlocked subjects, class limits, and seat limits.

## Firestore Collections In Use

- `users/{email}`: user profile, role, class membership, streak, XP, written progress.
- `users/{email}/progress/{cardId}`: per-card mastery/progress.
- `assignments/{assignmentId}`: teacher-created assignments.
- `assignments/{assignmentId}/completions/{studentId}`: per-student completion records.
- `assignments/{assignmentId}/attempts/{studentId}`: per-student assignment attempt summaries.
- `nudges/{nudgeId}`: automated teacher/support messages to students.
- `curriculums/{subjectId}`: subject curriculum data.
- `flagged_content/{flagId}`: student content error reports.
- `licenses/{licenseId}`: school/license/seat/class entitlement data.
- `licenses/{licenseId}/approved_students/{email}`: approved student school emails and allocated seats.
- `teacher_access_codes/{code}`: one-time lead teacher pilot invite codes.
- `class_join_codes/{code}`: 60 minute student join codes.
- `class_invites/{inviteId}`: shared teacher class invitations.

## Important Deployment Notes

1. Push commits to GitHub:
   `git push origin main`

2. Vercel should redeploy from GitHub automatically. If it does not, trigger a redeploy in the Vercel project.

3. Vercel Environment Variables must include:
   - `REACT_APP_SUPER_ADMIN_KEY`

4. Local development should use `.env.local` for the same variable. `.env.local` is not part of the safe code backup.

5. If Firestore rules change, deploy with:
   `npx firebase-tools@latest deploy --only firestore:rules --project dt-study-hub`

## Known Unfinished Tasks / Next Checks

1. Deploy the latest local commit to GitHub/Vercel.
   Codex saved the code locally, but GitHub push is blocked until the terminal has working GitHub authentication.

2. Run one controlled live smoke test.
   Use one lead teacher, one shared teacher, one approved student, one 60 minute join code, one assignment, and one flagged question.

3. Full Firebase rules testing.
   The source-level security test is green, but a proper Firebase emulator rules suite should be added before public launch.

4. Bulk curriculum import needs real dataset QA.
   The import foundation exists, but the final production workflow should be tested with a full AQA/real exam-board dataset, including chapter/subsection IDs, long-answer questions, mark schemes, image URLs, and version naming.

5. License onboarding is still manual.
   Lead teacher codes and student approvals work for the free pilot route, but school purchase, IT admin provisioning, and atomic seat allocation are not a polished self-serve onboarding flow yet.

6. Visual QA still needs real device testing.
   Tables were improved for laptop/mobile behavior and the build passes, but real iPhone, Android, Mac, Windows, and tablet screenshots should still be checked before a school demo.

7. Admin simulation is intentionally local-state only.
   This keeps production metrics clean. If persistent simulation scenarios are ever needed, they should go into a separate sandbox collection/project, not production student metrics.

8. True automated nudges need a backend scheduler.
   The app now stores rules, templates, and timing limits. Actual scheduled sending is a later Cloud Functions/Cloud Scheduler upgrade and needs Blaze.

9. Backups and legal/data paperwork are not finished.
   Manual export is enough for a tiny trial. Wider use needs Firebase backup/PITR planning, retention policy, privacy notice, and school data-processing agreement.

## Safe Restore Summary

To restore this checkpoint:

1. Clone or unzip the saved app source.
2. Run `npm install` or `pnpm install`.
3. Add `.env.local` with `REACT_APP_SUPER_ADMIN_KEY`.
4. Run `npm run build` to verify.
5. Push to GitHub and let Vercel deploy.
6. Confirm Firestore rules are deployed to `dt-study-hub`.
